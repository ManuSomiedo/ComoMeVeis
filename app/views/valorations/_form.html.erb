<%= form_for([@user,@valoration]) do |f| %>
  <% if @valoration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@valoration.errors.count, "error") %> prohibited this valoration from being saved:</h2>

      <ul>
      <% @valoration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <h2>You're currently evaluating the user :<%=@user.name%></h2>
  <div class="field">
    <%= f.label :"Hability name:" %><br>
    <%= f.select :hability_id, Hability.all.collect { |h| [ h.name, h.id ] }, selected: 1%>
  </div>
  <div class="field">
    <%= f.label :points %><br>
    <%= f.number_field :points %>
  </div>
  <%= f.hidden_field(:date, value: Time.now)%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
